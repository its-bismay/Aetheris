const DiffView = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Model Output Diff View</h1>
      <p>This is the model output diff view. Compare outputs between different models or versions here.</p>
    </div>
  );
};

export default DiffView;
